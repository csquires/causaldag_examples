The following command will create a virtual environment with the necessary packages to run the examples:

`bash setup.sh`

Activate the virtual environment:
`source venv/bin/activate`

Start jupyter:
`jupyter notebook`